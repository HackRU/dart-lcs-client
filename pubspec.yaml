name: lcs_dart_client
description: The Official dart client for the HackRU back end
version: 0.0.0
author: HackRU RnD Flutter Team
homepage: https://github.com/HackRU/dart-lcs-client

environment:
  sdk: ">=2.1.0-dev.68.0 <3.0.0"
  flutter: ">=1.2.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  http: ^0.11.3+16
#  http: ^0.12.0+1
  flutter_linkify: ^1.0.3
  url_launcher: ^5.0.1
  flutter_bloc: ^0.6.3
  bloc: ^0.9.3
  equatable: ^0.2.0
  meta: ^1.1.6
  http_middleware: ^1.0.0
  intl: ^0.15.7
  path_provider: ^0.5.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter